# FastAPI e servidor
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# PostgreSQL
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.25

# Processamento de dados
pandas==2.2.0

# LangChain e IA
# O langchain-google-genai precisa ser >= 3.1 para devolver as thought
# signatures que o Gemini 3 exige no function calling multi-turno. Isso arrasta
# langchain-core 1.x, que por sua vez obriga langchain-chroma e chromadb 1.x.
langchain-google-genai==4.3.7
langchain-chroma==1.1.0
chromadb==1.3.5

# Streaming SSE
sse-starlette==2.2.1

# Validação e configuração
pydantic>=2.7.4,<3.0.0
pydantic-settings>=2.1.0
python-dotenv==1.0.0

# Testes
pytest==8.3.4
pytest-asyncio==0.25.2
